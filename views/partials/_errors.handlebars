{{!-- Passport looks for 'error' from server side --}}
{{#if error}}
    <div class="alert alert-danger">{{error}}</div>

{{/if}}

{{!-- Errors is errors we have created  --}}
{{#each errors}}
  <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}